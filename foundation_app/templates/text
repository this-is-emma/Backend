<!DOCTYPE html>
<html>
    <head>
        <title>Welcome to the HOPE FOUNDATION</title>
        <style>
            body {
              max-width: 900px;
              margin: auto;
              text-align: center;
              height: 100%;
              background: rgb(254, 243, 228);
              background-repeat:repeat-x;
              background-position:bottom;
              background-size: 600px;
              padding: 10px;
              color:#ff8400;
              font-size: 25px;
              font-family: 'Gill Sans', 'Gill Sans MT', Calibri, 'Trebuchet MS', sans-serif;
            }

            a:link, a:visited {
            background-color: #ff8400;
            color: white;
            padding: 14px 25px;
            text-align: center;
            text-decoration: none;
            display: inline-block;
            }

            a:hover, a:active {
            background-color: white;
            color: #ff8400;
            }
            
            .content {
              background-color: rgba(255, 231, 200, 0.549);
              color:black;
              margin: 25px;
              padding: 25px;
              display: grid;
              grid-template-columns: 1fr 1fr 1fr;
              grid-gap: 20px;
              font-size: 20px;
              border: 1px solid rgb(255, 183, 128);
              border-radius: 5px;
            }

            .campaigns {
              border: 1px solid #fba447;
              border-radius: 5px;
              background-color: #ffd2a3;
              display: flex;
              flex-direction: column;
              padding: 25px;

              border: 1px solid rgba(0, 0, 0, 0.233);
    border-radius: 8px;
    box-shadow: 0 8px 15px rgba(0, 0, 0, 0.2);
    padding: 0.5rem 1.8rem;
    margin: 50px 40px;
    padding-bottom: 30px;
            }

            #campaign-name {
              color: #ff8400;
            }

            .total-donations {
              border: 1px solid #ffd0d0;
              margin: 10px;
              padding:10px;
              margin-top: auto;
              background-color: #d45d69b5;
              border-radius: 5px;
            }

            .form-div {
              border: 1px solid #fba447;
              border-radius: 5px;
              background-color: #ffd3a3;
              margin: 10px;
              padding:20px;
              padding-top: 150px;
              width:800px;
              height: 200px;
            }

            .user-account{
              border: 1px solid #fba447;
              border-radius: 5px;
              background-color: #ffd3a386;
              display: flex;
              flex-direction: column;
              margin: 10px;
              padding:20px;
            }

            .user-account > a {
                margin: 10px;
                padding:20px;
            }

            .my-donations{
              border: 1px solid #fba447;
              border-radius: 5px;
              background-color: #ffe7ce;
              color: rgb(177, 0, 0);
              margin: 10px;
              padding:20px;
            }

            .flash-message {
              background-color: white;
              font-size: 30px;
              color: green;
            }
            </style>
    </head>
    <body>
        <header class="header">
          <h1>Hope Foundation</h1> 
          {% for message in get_flashed_messages() %}
              <p class="flash-message">{{ message }}</p>
          {% endfor %}
          <nav>
              {% if current_user.is_authenticated %}
                  <a href="/">Home</a>
                  <a href="/user_account">My account</a>
                  <a href="/new_campaign">Create a campaign</a>
                  <a href="/donate">DONATE</a>
                  <a href="/logout">Log Out</a>
              {% else %}
                  <a href="/">Home</a>
                  <a href="/login">Log in</a>
                  <a href="/signup">Sign up as a donor</a>
              {% endif %}
          </nav>
        </header>
        <div id="all">
        
            {% block content %}{% endblock %}
                
        </div>
    </body>
</html>
